* - Data Server Handler
** - Cental Server Handler

* INSERTINNODE filename1 key serverid filename2
	ptr = {'serverID': serverid, 'filename': filename2}
	call insertInNode(fileName1, key, ptr)

* SAVELEAF filename leafcontent
	l = Leaf()
	destringifyLeaf(leafcontent, l)
	l.printToFile(filename)


* SAVENODE filename nodecontent
	n = Node()
	destringifyNode(nodecontent, n)
	n.printToFile(filename)

** CHANGEROOT serverid filename
	open metadata
	update root
	save

** NEWNODE key
	call bestServer function
	? create actual file?
	get serverID, filename
	return sid, filename

** NEWLEAF key
	call bestServer function
	? create actual file?
	get serverID, filename
	return sid, filename

** WHOISROOT
	read metadata
	return sid, filename

* CHANGELEFTPTR filename1 sid filename2
	l = Leaf()
	load filename1 in l
	change left pointer to sid, fname2
	save

* CHANGEPARENT filename1 sid filename2
	see if filename1 is leaf or node
	accordingly load in Node() or Leaf()
	change parent to sid, filename2
	save

* FINDLEAF key filename
	fire up findleaf function as it is 
